# TODO / Action Items

Living tracker — updated whenever there's something you need to action, or something I need to remember for next session. Not a changelog; see `git log` for history.

_Last updated: 2026-06-23_

## 🟢 In progress — pick up next session

- [ ] **Drill-down capability (national → state → suburb) — Phase 1 shipped (State → SA2), Phase 2/3 open.**

  Goal (from brainstorm): a "3 clicks from country to block" experience for a national-leader-style demo/pitch. The original framing — exact ambulance counts at city-block level — isn't buildable: no Australian ambulance service publishes fleet/availability data publicly, same class of gap as the DNSP outage maps and Pillar 2 EOC data noted below. Reframed (with sign-off) as: real ABS boundary drill-down + real live signals filtered to whatever's selected, never fabricated numbers.

  ✅ **Phase 1 shipped**: `server/routes/boundaries.js` proxies ABS's free ASGS2021 GeoJSON service (no API key, CC BY 4.0, confirmed live — and CORS-blocked, so it must stay server-side, not called from the browser) for State and SA2 (suburb) boundaries, cached in memory. `MapCenter.jsx` renders them as clickable fill/line layers: click a state to zoom in + load its SA2 suburbs, click a suburb to zoom further. `DrillPanel.jsx` shows a breadcrumb (click any level to go back) plus the boundary's real area (km², from ABS) and live counts of emergency incidents/road closures/fires/floods falling inside it, via a new point-in-polygon helper (`src/utils/geo.js`). Verified end-to-end with a headless browser this session (state click → suburb click → breadcrumb reset, all confirmed with real numbers, not mocked).

  ⬜ **Phase 2 (not built)**: true block-level (SA1, ~200–800 people — the actual "block" granularity ABS offers) as a third click; a public hospital-location dataset (data.gov.au has one) for an honest "nearest emergency facility" data point instead of the impossible ambulance-count ask; polish the zoom/transition animation for demo smoothness.

  ⬜ **Phase 3 (optional, unresearched)**: whether any state health department publishes ED wait-time/ambulance-ramping data publicly as a coarser but real proxy for "emergency capacity." Skip rather than fabricate if nothing real turns up.

  **Known gaps in Phase 1**: no automated tests yet for `boundaries.js` or `geo.js`. Stat counts can read oddly for very large/sparse SA2s (e.g. "Outback" in SA covers >500,000km² with near-zero signal density) — fine for a demo, but Phase 2's SA1 granularity would make dense urban areas feel more meaningful.

- [ ] **#3 Incident Impact Assessment (area + people affected) — first slice shipped (emergencyAlerts only), other 7 categories still open.**

  Goal: every incident surfaced in the Threat Index (energy outage, road closure, port congestion, flood, fire, etc.) should report an estimated **impacted area** and **number of people affected**, not just a severity level — e.g. "LOR2 in SA Region — ~1.8M people in at-risk area" instead of just "ORANGE."

  ✅ **Shipped for `emergencyAlerts` only** (scoped down via direct Q&A, not the full plan below): `server/routes/emergencyImpact.js` computes a deterministic area/population estimate per incident — distance-to-nearest-capital-city banding (a simplified stand-in for ABS Remoteness Areas, **not** the real ABS SA2 boundary dataset) × a severity-scaled buffer radius (3/7/15/25km) — aggregated into one category-level figure shown in the Early Warning panel. For severity ≥2 (Watch and Act/Emergency Warning) incidents only, also generates a one-sentence AI infrastructure/traffic narrative (reusing the Gemini/GPT-4o/OpenRouter fallback chain from `aiBrief.js`), explicitly labeled "[AI estimate]" in the UI and never used for the headline numbers. Independent ~12 min poll, decoupled from the 60s Threat Index cycle. `abs.js` still only has *national* population (27.8M) — no state/SA2 breakdown — so this deliberately avoids Phase 1/2 below in favor of a lighter proxy. Road closures, flood, fire, energy, port congestion, cyber, and seismic remain uncovered — the phased plan below still applies to those.

  **Coverage gap research (follow-up pass — TNSPs, WA-WEM, NT, state emergency feeds):**
  - **TNSPs (Powerlink/TransGrid/AusNet/ElectraNet/TasNetworks) — partially resolved.** AEMO confirms all 5 TNSPs submit planned/unplanned transmission outages to AEMO (via the Network Outage Scheduler or a monthly spreadsheet for ElectraNet/AusNet/Powerlink) — so it's centrally aggregated, not 5 separate integrations. Found the actual feed: `nemweb.com.au/Reports/Current/Network/` publishes a `PUBLIC_NETWORK_*.zip` (CSV inside) every ~30 min, no key needed. **But** the one archive I opened this session (`unzip` + inspect) contained only `EQUIPMENTDETAIL`/`SUBSTATIONDETAIL`/`OUTAGESTATUSCODE` — static network reference data, not live outage *events*. The actual live-outage table (likely `NETWORK_OUTAGEDETAIL` or similar in AEMO's MMS Data Model) wasn't located yet — needs one more research pass checking AEMO's MMS Data Model doc for the exact table/report name, or checking other timestamps in the same directory (file content may vary by generation time).
  - **WA-WEM — still unresolved.** AEMO confirmed to administer the WEM (separate market from the NEM) for WA's SWIS, with its own "Market Notices"/"Dispatch and market notices" pages — but `aemo.com.au` is Cloudflare-protected and returned 403 to every fetch attempt this session. Never found whether a nemweb-style public file directory exists for WEM (e.g. a `wa.nemweb.com.au` analog). Needs a fresh attempt, possibly via a real browser session instead of automated fetch.
  - **NT — confirmed dead end.** Power and Water Corporation (`powerwater.com.au/outages`) is the only public source and it's a web page with no documented API; no AEMO oversight exists for NT's isolated grid at all. Territory Generation/Jacana Energy (NT retailer) also web-only. Unless PWC has an undocumented endpoint behind their outage map (untested — would need the same browser-automation approach already blocked this session for DNSPs), this gap likely stays a gap.
  - **State emergency services — resolved separately from impact estimation, see ✅ entry in Build backlog below.** Original concern was power-outage data specifically: none of these feeds carry customer/outage counts, so they remain a "storm/disaster happened here" cross-signal (corroborating an AEMO notice, or catching outages AEMO never formally notices about), not an impact-estimation source.
  - Individual generator/wholesaler outages (AGL, Origin, EnergyAustralia, Snowy Hydro, Stanwell, CS Energy, etc.) — still unverified by example; AEMO's Market Notices almost certainly cover their forced outages since AEMO is the system operator, but no sample notice was pulled to confirm format/detail.

  **Why impact estimation is hard, by category:**
  - Electricity (AEMO LOR notices): finest granularity in the notice text is the **NEM region** (whole state, e.g. "SA Region") — AEMO doesn't say which suburbs would actually be load-shed (the DNSP decides that in real time, rotated). So an honest impact number here is "population of the at-risk state," not "people who lost power" — the UI must not conflate a forecast/risk notice with an actual outage.
  - Road closures (TfNSW): we already have lat/lon + suburb per closure — best category to start with for a real area+population estimate (buffer radius around the point, intersect with population data).
  - Port congestion: "impact" is more about freight/economic disruption than resident population — probably needs a different metric (e.g. cargo value/throughput) rather than forcing a population number that doesn't mean anything here.
  - Flood/fire: BOM/NASA data already carries some geographic extent (polygon or point+radius) — worth reusing the same impact model once electricity/roads are sorted.

  **Possible phased approach (needs your sign-off before building):**
  1. **Phase 1 (cheap, buildable now):** region/state-level population exposure — map NEM region → state → ABS population (we already pull ABS data in `abs.js`). Must be labeled "population in at-risk region," not "people without power," to avoid being misleading.
  2. **Phase 2 (medium):** for point-located incidents (road closures, fires, floods), buffer the point by a radius and intersect with ABS SA2-level population estimates for a real area+population figure.
  3. **Phase 3 (blocked on existing gap):** true per-customer outage counts — needs live DNSP outage data, already noted above as inaccessible without browser-automation reverse-engineering or a paid data agreement.

  **Open questions for you before this gets built:**
  - Is Phase 1 (coarse, state-level "population at risk") good enough to ship first, with a clear caveat that it's not literal "people without power"?
  - Build this for every Threat Index category, or electricity-first?
  - Any appetite for the DNSP browser-automation route (Phase 3) despite the ToS-grey area, or AEMO-only sources for now?
  - Want me to research the missing TNSP/WA-WEM/NT coverage above before or alongside building impact estimation?

---

## 🔴 Needs your action

- [ ] **Register a free TfNSW Open Data Hub API key** and set `TRANSPORT_NSW_KEY` in `.env`.
  Sign up at https://opendata.transport.nsw.gov.au → My Account → Applications → create one → API Management → add "Live Traffic Hazards" → copy the key.
  Without this, the Road Closures map layer stays empty (fails silently, just logs a warning server-side).
- [ ] **Rotate your OpenRouter API key.** It was pasted in plaintext in this chat early in the session (low risk — free tier, no billing exposure — but still sitting in conversation history). Regenerate in the OpenRouter dashboard and update `OPENROUTER_KEY` in `.env`. (Checked 2026-06-23: `OPENROUTER_KEY` is set/non-empty in `.env`, but a value's presence doesn't confirm it's the *new* rotated one — please confirm directly.)
- [ ] **Street View, if you still want it.** Confirmed this session it doesn't need a map-engine migration — keep the main map on MapLibre/CartoDB and add a Street View panel/modal alongside it. Needs a Google Cloud account + Maps JavaScript API enabled + billing set up (Google gives $200/month free credit, same friction level as the TfNSW/OpenRouter keys above). Once you have a key, tell me and I'll wire in the panel.

## 🟡 Known gaps, not urgent

- [ ] `GPT4O_KEY`, `FR24_API_KEY`, `ACLED_KEY` are empty in `.env` — pre-existing, app runs fine without them (Gemini/OpenRouter cover AI brief, OpenSky covers flights). Only fill these in if you want those specific providers active.
- [ ] Reservoir/dam storage levels remain **static placeholder data** (not live) — no public API exists for this (see session notes below). Revisit only if WaterNSW/Seqwater/Melbourne Water offer direct API access in future.
- [ ] **No live localized blackout counts (electricity DNSPs).** Ausgrid/Energex/SA Power Networks/etc. all run live outage maps but none expose a documented public JSON API — their maps are JS-rendered SPAs and I couldn't get a working headless-browser session this session to find the underlying calls. Would need either a working browser-automation pass to reverse-engineer one DNSP's map (per-state, fragile, ToS-grey) or a paid/negotiated data-sharing agreement. Skipped for now — AEMO's system-wide notices (below) substitute as the national signal.
- [ ] **No live diesel/petrol supply-disruption feed.** Only thing close is NSW FuelCheck (free API key, `data.nsw.gov.au`) — but it's live *prices* only, NSW-only, not a national outage/shortage signal. Currently covered only via a news-keyword catch (`FUEL_SHORTAGE_RE` in `threatIndex.js`). If you want NSW fuel price data as a weak proxy, register a free key at https://api.nsw.gov.au and set it in `.env` — tell me and I'll wire it in.
- [ ] **No public incident feed for NSW SES, SA SES, or NT PFES.** Checked directly (not just via EmergencyAPI.com) — NSW SES requires emailing `warnings.capability@ses.nsw.gov.au` for API access; SA SES has only a web map, no feed distinct from CFS's fire-only feed; NT PFES has only a Google-Maps-based incident map page, no RSS/JSON/KML endpoint found. All three are missing from the `emergencyAlerts` map layer/Threat Index category as a result. Revisit only if one of these agencies publishes a documented public feed in future.
- [ ] **NSW and VIC direct emergency feeds under-report vs. EmergencyAPI.com, unexplained.** `server/routes/emergencyCrossCheck.js`'s first live run found: NSW direct=13 vs EmergencyAPI(RFS only)=26 — same underlying feed, 2x gap; VIC direct=6 vs EmergencyAPI(EMV+EMV-FIRE+EMV-FLOOD+SES)=112 — much larger gap. WA (81 vs 85) and TAS (1 vs 1) matched closely on the same comparison, so this looks specific to NSW/VIC, not a flaw in the cross-check method itself. Possible causes not yet investigated: a parsing bug dropping records in `emergencyAlertSources.js`'s `parseNswRfs`/`parseVicEmergency`, a freshness/timing skew between polls, or EmergencyAPI counting retracted/historical items we correctly exclude. Deliberately deferred — revisit when there's budget for it.

## 🔵 Build backlog (Dashboard 2.0, prioritized)

From the pillar review — ✅ done, ⬜ not started:

- ✅ **Threat Level Index (Pillar 4)** — composite score from seismic/weather/fire/flood/cyber/infrastructure/energy, with drilldown.
- ✅ **Road closures (Pillar 1)** — TfNSW Live Traffic Hazards, map layer + popups (pending your API key).
- ⬜ **Reservoir levels (Pillar 1)** — skipped, no reliable public API found.
- ✅ **Port congestion metric (Pillar 1)** — `server/routes/portCongestion.js` derives idle/anchored vessel counts (AIS status 1/5) within 20km of each of the 11 major seaports from existing AIS ship data, polled every 60s.
- ✅ **Fold infrastructure into Threat Index** — `threatIndex.js` now has an `infrastructure` category combining road closures + port congestion, exposed via `/api/threat-index` and the map's Early Warning panel.
- ✅ **Energy outage monitoring (electricity/gas)** — `server/routes/energyOutages.js` polls AEMO's public NEM Market Notices (LOR1/2/3 reserve notices, power system events, directions, market suspension — no API key) every 5 min, and AEMO's East Coast Gas System notice CSV every 30 min. Folded into `threatIndex.js` as a new `energy` category (electricity LOR2/3 escalates to ORANGE/RED). Diesel/petrol not covered — see gaps above.
- ✅ **Direct state/territory emergency incident feeds** — `server/routes/emergencyAlertSources.js` pulls official, direct (no API key, no third-party wrapper) public feeds from NSW RFS (GeoJSON), VIC CFA/SES/DELWP combined (JSON), QLD QFES (GeoJSON), SA CFS (JSON), WA DFES (RSS incidents + graded warnings), TAS TFS (KML), and ACT ESA (GeoRSS), normalized into one incident shape (`{ state, agency, category, title, status, severity 0–3, lat, lon }`). Polled every 5 min by `server/routes/emergencyAlerts.js`, exposed at `/api/emergency-alerts`, rendered as a new "Emergency Incidents" map layer (severity-colored pins), and folded into `threatIndex.js` as an `emergencyAlerts` category. NSW SES/SA SES/NT PFES have no public feed — see gap above. This is the first feature in the project with unit tests (`vitest`, `npm run test`) — see fixtures in `emergencyAlertSources.test.js` and `threatIndex.test.js`.
- ⬜ **Incident impact assessment (area + people affected)** — see "In progress" section above for full plan, coverage gaps, and open questions. Unaffected by the emergency-incident-feed work above — that's a cross-signal, not an impact/population source.
- ⬜ **Grid instability proxy (Pillar 4)** — AEMO spot-price spike as a substitute signal for grid frequency (which isn't public); reuses existing `energy.js` data. Distinct from the outage-notice work above — this would be a price-volatility signal, not an outage event.
- ⬜ **Sentiment/misinformation tracking (Pillar 3, scoped)** — Reddit + Google Trends (skip X/Twitter, now paid); reuse existing Gemini/OpenRouter pipeline for classification.
- ❌ **Public Health & Humanitarian (Pillar 2)** — dropped; no public data source exists in Australia for any of its KPIs.

## 📝 Session notes / decisions worth remembering

- This app is 100% public-data OSINT — no SCADA/utility/telco/911 internal access. Anything modeled on internal Emergency Operations Center data (Pillar 2 in full) is a non-starter.
- WaterNSW dam-levels page has no exposed API (server-rendered HTML only) — don't revisit scraping it without a structural change on their end.
- BOM's official water data API (SOS2/XML) is explicitly "not near-real-time" and complex — same reason fire-danger ratings already fall back to a seasonal guess.
- Port congestion thresholds (`portCongestion.js`) are first-pass guesses, not calibrated against real port traffic: 20km radius, level escalates at 1/3/6/10 idle (anchored/moored) vessels. Revisit if congestion levels look obviously wrong once live AIS data is observed over a few days — e.g. some bulk ports (Port Hedland, Hay Point) routinely queue more ships than container ports, so a flat threshold may need to be per-port.
- AEMO Market Notice files always set `Notice Type ID` to a generic bucket (e.g. "RESERVE NOTICE" covers LOR1/2/3 alike) — the actual severity (which LOR level, market suspension, etc.) only shows up in the free-text `Reason` narrative, not a clean enum. `scoreEnergy()` in `threatIndex.js` regex-matches the combined type+reason text for this reason — same pattern as the existing cyber-news classifier. Confirmed working against a live LOR3 (SA) notice during this session, correctly drove overall index to RED.
- **CSS Grid "blowout" footgun** (found fixing the mobile layout): a track sized just `1fr` — or an *implicit* column on a container with no `grid-template-columns` at all — inherits its automatic minimum size from its widest descendant's min-content. Concretely: 5 nowrap finance tabs forced a 390px-wide mobile container to internally compute as 605px, silently clipped by `overflow:hidden` rather than erroring or scrolling. Fix is `minmax(0, 1fr)` at *every* nested grid level — both `app-shell` and `body-grid` needed it, fixing only one wasn't enough. If the desktop 3-column layout (`1fr 45% 1fr`) is ever squeezed very narrow (small laptop window), the same bug class could reappear there — it currently only has the fix on mobile's single-column override.
- **Test coverage status**: `financial.js`, `portCongestion.js`, and all of `threatIndex.js`'s categories now have tests (89 total, up from 33). ~19 of 26 server routes are still untested — mostly thin "fetch external API → return JSON" routes (weather, fids, seismic, etc.) with low value-per-test. If resuming this, `energy.js`/`energyOutages.js`/`abs.js` (real parsing logic) are better next candidates than the thin proxies.
